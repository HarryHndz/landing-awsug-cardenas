# URL base (invoke URL) de tu API Gateway, incluyendo el stage.
# Ejemplo: https://xxxxxxxxxx.execute-api.us-east-1.amazonaws.com/prod
# El código hace fetch a `${VITE_API_URL}/events`.
VITE_API_URL=
